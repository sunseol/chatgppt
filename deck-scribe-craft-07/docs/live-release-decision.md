# Live Release Decision

Path: `docs/live-release-decision.md`

Date: 2026-06-21

Release decision: Blocked

## Decision basis

The current worktree has stronger Live-readiness contracts than the previous mock-provider build: production mock selection is blocked by policy, provider provenance and structured Codex output gates exist, research fetches preserve capture metadata, image provider responses preserve request metadata, live generation report lineage is checked per slide, Golden Path E2E and manual QA evidence bundles have local verifiers, the Packaged Live evidence index validator now ties every remaining open P0 evidence pointer together, DF-247 final release gate criteria are encoded, and the final export gate rejects contaminated production lineage. It still does not yet have enough Live evidence to approve the Live Initial Version.

## Blocking facts

- Remaining P0 Live tickets are not all Verified Live; DF-205, DF-233, DF-235, DF-241, DF-242, DF-243, DF-244, DF-245, DF-246, and DF-247 still require committed live evidence before release.
- Production package mock execution paths are blocked at the policy layer and the current dry-run package scan no longer finds mock provider or mock stage resources.
- Live benchmark pass count is 0 of 5, below the required 4 of 5.
- Golden Path lineage with zero mock or fixture artifacts, text turn ids, image provider turn ids, prompt versions, and compositor-matched export hashes is enforced by local gates but has not been produced by a real run.
- The shared Packaged Live evidence index now exists at `docs/live-evidence/release/packaged-live-evidence-index.json`, but every entry is still `blocked` because auth/secret lifecycle, image queue, slide regeneration, usage disclosure, signed Golden Path, benchmark, interruption, packaging, manual QA, and release-gate evidence are not ready.
- Clean-machine Codex login, image credential setup, live interview, live research, live image generation, title edit, and export have not been recorded.
- The current DMG is unsigned and not notarized.

## Latest local verification

- `bun test` passes: 1156 tests, 0 failures, 4396 expectations.
- `bun run typecheck` passes.
- `bun run lint` exits 0 with six pre-existing React Fast Refresh warnings in
  shared UI files.
- `bun run build:package` passes for client, SSR, prerendered `/`, and package-build sanitization.
- `bun run package:dry-run` creates `dist/deckforge-macos-dry-run.tgz`.
- Dry-run archive SHA-256: `e6ed0e25791dd51a1c206247bd0faf5a1010aaee6c7b16e7256dfd25f74f47f6`; 285,197 bytes, 27 archive members, 18 app files.
- Dry-run packaging is deterministic: `scripts/package-dry-run.mjs` writes a sorted fixed-metadata tar/gzip archive, `scripts/package-path-sanitizer.mjs` normalizes prerendered route-match timestamps, and two consecutive `bun run package:dry-run` runs produced the same SHA-256 above.
- Current unsigned DMG SHA-256: `d6849d24c5af4548b7b35e65a68a05c8d139be4b1b5504d7c3da3a3dc9e2d467`; 1,833,575 bytes.
- Current native app binary SHA-256: `f886f3dc8c9e5c968a7a5a80134814a72b629c9fdbf0bff05e570408a7003c65`.
- Native Tauri packaging now runs `bun run build:package`, so the TanStack generated `dist/server` manifest is sanitized after Tauri's `beforeBuildCommand` instead of being recreated with developer-local absolute paths. `scripts/package-path-sanitizer.test.mjs` covers that wiring.
- Package content scan finds no hits for `mock-provider`, `MOCK MODE`, `.omx`, `.playwright-mcp`, local developer absolute paths, `CODEX_SESSION=`, assigned `OPENAI_API_KEY`, or bundled `auth.json` in `dist/client`, `dist/server`, the extracted dry-run app bundle, or the native `.app`. OpenAI key-shaped regex hits are Tailwind CSS utility identifiers such as `sk-image-linear-from-pos`, not secrets.

## 2026-06-21 Lane F Release-Gates Recheck

Lane F ran the current branch `jacobex/live-lane-release-gates` from `/Users/jake/chatgppt-lane-release-gates/deck-scribe-craft-07`. This was a developer worktree run, not a clean macOS account or non-developer QA session.

Evidence produced:

- `bun install --frozen-lockfile` installed dependencies from `bun.lock`.
- `bun test` passed: 991 tests, 0 failures, 3853 expect calls.
- `bun run typecheck` passed.
- `bun run lint` passed with the six pre-existing React Fast Refresh warnings in shared UI files.
- `bun run build` passed for client, SSR, and prerendered `/`.
- `bun run package:dry-run` produced `dist/deckforge-macos-dry-run.tgz`, SHA-256 `cec0077d117f8cc2d863db2075bbbd55cc812830e91233474a9f550ee6de427b`, 287,894 bytes, 17 app files.
- `bun run tauri:build` produced `src-tauri/target/release/bundle/macos/DeckForge.app` and `src-tauri/target/release/bundle/dmg/DeckForge_0.1.0_aarch64.dmg`; the DMG was copied to `release-artifacts/DeckForge_0.1.0_aarch64.dmg`.
- `shasum -a 256 -c release-artifacts/DeckForge_0.1.0_aarch64.dmg.sha256` passed for DMG SHA-256 `232d0fd67eed137ff8b048848823d95cd71f2c8cd044a07ba279defd0a934108`.
- `bun run verify` passed: typecheck, 991 tests, production build.
- `bun run rust:fmt && bun run rust:clippy && bun run rust:test` passed; Rust tests reported 5 passed, 0 failed.
- Best available isolated smoke used a temporary HOME at `/tmp/deckforge-clean-home.Jlj63s`, started the internal dry-run package on port 4179, fetched `/`, and received a 12,596-byte HTML response. This is not clean-machine release evidence because it used the current developer account and unsigned dry-run package.

Release remains `Blocked`. No ticket in the Lane F set can honestly close from this run because the required live Golden Path bundle, benchmark bundle set, complete interruption matrix, Developer ID signing/notarization/stapling/Gatekeeper acceptance, clean-machine run, non-developer manual QA session, and final release-gate evidence are still missing.

## Known limits

- Codex CLI is available locally through the standalone 0.141.0 install, ChatGPT login is present, and `docs/live_text_smoke_report.md` records generated App Server protocol schemas, a successful `codex app-server --stdio` JSON-RPC `initialize` handshake, persistent daemon bootstrap, an authenticated App Server health turn, a resumed second turn, a forced daemon crash, daemon restart, a completed post-restart health turn, and schema-constrained structured probe turns. The original structured probe completed thread `019edb09-7568-70e2-a74c-5f2cc47e48fc` and turn `019edb09-77f6-7332-a8d1-e9f6240bf331` with output SHA-256 `0a9d15bd5720bde9ffc8f5a42d38e4bd37b9099bb3d8d328ff3e8349959a955d`; the current recheck completed thread `019edb32-07eb-7902-85bd-04823b1c47c2` and turn `019edb32-0a20-7812-ba4b-8603beb1b4aa` with output SHA-256 `d165df896f4a80c9bbecaa36262b1b5540adc11591c0138ebea93b1dcb773b4c`; the goal-continuation recheck completed thread `019edbdc-61ca-7fc1-9cb4-9146ef9a1237` and turn `019edbdc-6472-7252-a846-334f23436989` with output SHA-256 `8b579a44dbdd4d90050087ecbc8be606ba407d53a9893effd8961a64b8db5c68`. Library-level desktop workflows have now completed live `questions` and `brief` artifacts plus live Deck Plan, Design System, and Layout IR artifacts through authenticated App Server structured turns. The actual packaged DeckForge production UI still has not completed interview through Layout IR with live Codex-only artifacts, and clean-machine reproduction evidence is still missing.
- A DF-210 stdio shutdown/channel cleanup probe on the current standalone `codex-cli 0.141.0` binary initialized App Server, read authenticated ChatGPT account state, emitted 3 parseable stdout protocol JSON lines and 0 stderr lines, then exited with code `0` and signal `null` 4 ms after stdin close. Evidence digest: `f35e49556578ced8eedb4f6fbe5dcdf8ee742863037c7787166cd1d4232eb1cd`. The desktop smoke and structured-turn bridge evidence now carries `protocolLineCount` and `stderrLogLineCount` from the Tauri session, so app-produced evidence preserves stdout protocol and stderr log channel separation; desktop smoke results now fail as `invalid_smoke_evidence` unless initialize succeeds, an authenticated account is present, a completed protocol health turn is observed, nonblank thread/turn ids are present, and stdout protocol frames include `turn/completed`. Desktop structured-turn results now fail as `invalid_structured_turn_evidence` unless runtime, thread id, turn id, `turnCompleted`, stdout protocol frames, event methods, and preserved notifications prove a completed `turn/completed` turn. The initialize evaluator rejects missing protocol identity fields and stderr JSON-RPC protocol frames, the core health-turn evaluator rejects blank or non-canonical completed health turns and completed turns from non-ChatGPT account modes before they can count as authenticated evidence, and the restart smoke gate rejects same-pid restarts, blank crash probes, blank or non-canonical post-restart thread/turn ids, CLI-version mismatches, blank or CLI-mismatched `appServerVersion`, and whitespace-padded reused pre-restart health turns. This strengthens local process cleanup and restart evidence but still does not replace clean macOS account reproduction.
- Production image generation is now guarded as a Codex OAuth-only route even after project reload: `createProductionImageGenerationGate` blocks persisted `openaiImage` / `openaiApiKey` decisions with `production_codex_oauth_required`, and also blocks stale locked Codex decisions with `provider_auth_required` when the current provider status requires login, so an old API-key image artifact or post-logout Codex decision cannot become a production-ready route.
- Local project folder export now includes project-scoped browser artifact writes from the image artifact store, so packaged QA can extract product-written queue, interruption, image, and image-billing confirmation artifacts from the app surface. This removes an extraction gap only; DF-233, DF-243, and DF-244 still require real packaged Codex image/interruption runs whose exported artifacts are copied into `docs/live-evidence/...`.
- A DF-233 product queue-control smoke at `docs/live-evidence/codex-image/df233-queue-controls-smoke-20260622/summary.json` proves the product queue writer emits ready evidence for bounded 5xx retry provenance, in-flight cancellation that rejects a late provider output, and restart-resume proof that does not regenerate completed slides. This narrows the queue-control writer gap, but the release remains blocked until packaged Codex OAuth image jobs capture equivalent retry, cancellation, and restart-resume evidence from the app surface.
- `scripts/produce-df233-packaged-queue-evidence.ts` now provides the handoff
  surface for a real packaged image-queue bundle. It parses retry,
  cancellation, and restart-resume queue evidence copied into
  `docs/live-evidence/...`, requires the same packaged queue session and package
  hash, checks project-folder export coverage, and keeps blocked product
  queue-validation results or missing scenario proofs from becoming release
  evidence.
- A real Codex OAuth cancellation smoke at `docs/live-evidence/codex-image/df243-live-codex-cancel-smoke-20260622/summary.json` proves the product queue rejects a late App Server image after cancellation before storage, preserves no slide image artifacts, writes ready DF-233 queue evidence, and writes distinct DF-243 app-storage recovery and cancel-signal JSON for App Server thread `019eed0f-b516-7cc1-9b4d-f53ca1ec1d7c`, turn `019eed0f-b799-7f91-98d7-67617abcb758`. This narrows the real-provider cancellation gap, but the release remains blocked until packaged app evidence captures retry, cancel, restart-resume, image partial-resume, and interrupted approval/export artifacts.
- A DF-243 product queue cancellation smoke at `docs/live-evidence/codex-image/df243-cancel-product-smoke-20260622/summary.json` proves a late in-flight Codex image queue output is rejected after cancellation, writes separate app-storage recovery and cancel-signal JSON through the product cancel writer, and confirms local project folder export includes both artifacts. This narrows the cancellation writer gap, but the release remains blocked until the same interruption evidence is captured from the packaged app alongside image partial-resume and interrupted approval/export gate artifacts.
- A DF-243 product resume/gate smoke at `docs/live-evidence/codex-image/df243-resume-gate-product-smoke-20260622/summary.json` proves the product image partial-resume writer persists pending-to-resumed recovery evidence, the interrupted artifact gate writer persists distinct approval/export gate JSON, and local project folder export includes those three artifact classes. This narrows the resume/gate writer gap, but the release remains blocked until the same evidence is captured from the packaged app and copied into the canonical DF-243 closure bundle.
- `scripts/produce-df243-interruption-closure-evidence.ts` now provides the
  handoff surface for a real packaged interruption matrix. It derives the
  required DF-243 closure artifact paths from the matrix, builds the matching
  matrix payload, and runs `evaluateLiveInterruptionClosureEvidence` without
  treating local smoke artifacts as packaged release proof. Malformed or
  incomplete input remains blocked before it can be recorded as closure
  evidence.
- A DF-235 Review-stage failure-preservation smoke at `docs/live-evidence/codex-image/df235-review-failure-preservation-20260622.json` proves a failed live regeneration preserves the approved original slide and writes `preserved_after_failure` evidence through the product runner. This narrows the local regeneration writer gap, but packaged Review-stage approval and failure-preservation capture are still required.
- A DF-245 current package recheck at `docs/live-evidence/release/df245-package-recheck-20260622.json` ties the release evidence to the active dry-run archive SHA-256 `e6ed0e25791dd51a1c206247bd0faf5a1010aaee6c7b16e7256dfd25f74f47f6`, unsigned DMG SHA-256 `d6849d24c5af4548b7b35e65a68a05c8d139be4b1b5504d7c3da3a3dc9e2d467`, clean configured mock/secret/local-path scans, and current signing/Gatekeeper rejection output. The dry-run launcher now also serves emitted packaged JS/CSS assets from `Resources/client/assets` before falling back to SSR, and `docs/live-evidence/release/df245-release-trust-codesign-notarytool-stapler-spctl-20260622.json` persists the blocked `codesign`, `notarytool`, `stapler`, and `spctl` command results. This removes stale DF-245 hash drift and missing-blocker-record ambiguity, but does not unblock the release because Developer ID signing, accepted notarization, stapling, Gatekeeper acceptance, passed release-trust JSON, and clean-machine evidence are still missing.
- `scripts/produce-df245-production-packaging-evidence.ts` now provides the
  handoff surface for a real production package bundle. It parses package,
  native macOS bundle, release-trust, clean-machine, runtime-remediation, and
  runbook evidence, checks the package hash, then runs
  `evaluateProductionPackagingEvidence` before returning ready or blocked.
  Unsigned dry-run packages, missing release-trust payloads, and missing
  clean-machine evidence remain blocked.
- DF-205 now cites that same current-package recheck as active auth/secret lifecycle scan evidence for the dry-run archive and native app. It narrows DF-205 to clean-account login/logout/relogin, packaged Codex OAuth capability, packaged keychain lifecycle, and clean-machine secret leak evidence.
- `scripts/produce-df205-packaged-auth-secret-evidence.ts` now provides the
  handoff surface for a real clean-account packaged auth session. It parses
  fresh-login, logout/relogin, Codex OAuth image-capability, optional keychain
  fallback lifecycle, and packaged secret-scan evidence, then keeps package hash
  drift, missing provider lock, unavailable Codex image capability, incomplete
  keychain fallback proof, or secret-scan hits blocked.
- The defensive legacy image API-key secret-store path rejects unsupported non-keychain stores, raw/encoded secret material in returned references, wrong-scope/stale references, wrong-service disconnect references, and blank or boundary-whitespace-padded `LiveSecretReference` identity fields before a credential can be treated as stored or deleted.
- `src/lib/research-live-network-policy.ts` scopes live search to Research, quarantines fetched source content as `untrusted_source_content`, marks shell/credential/external POST injection attempts as non-executable hazards, and blocks production mock-source fallback. `src/lib/research-source-fetcher.ts` allows only GET/HEAD for URL-backed source fetches. `src/lib/live-web-search-evidence.ts` blocks cached/mock search candidates, missing search metadata, query/event-log mismatches, insufficient live-domain diversity, cached-only latestness benchmarks, and unknown benchmark candidate references. `ResearchPack.webSearchEvidence` preserves accepted search event logs through Research Pack parsing and approved Research Pack artifacts, and `src/components/deck/ProductionResearchWebSearchLauncher.tsx` now connects the production Research step to a desktop App Server `web_search` launcher that stores candidates plus production provenance. A live App Server repro found and removed an unsupported `format: "uri"` response-schema blocker for `web_search` candidate URLs; a later live worker rerun completed thread `019edc32-6c92-7371-b34d-e6e7858253db` and turn `019edc32-6efe-7280-a2c1-47fb1d6b0ebf` with six live candidates across six domains and no DF-221 blockers. Packaged Golden Path research/source-capture evidence is still missing.
- Codex image provider code maps App Server `imageGeneration` and raw `image_generation_call` output into PNG data URLs with Codex turn/thread provenance, `src/lib/image-path-decision.ts` blocks fixture fallback/unverified routes, incomplete or placeholder account-usage/permission decision metadata including mixed real/placeholder required-permission lists, mismatched providers, missing or mismatched request models, mismatched prompt versions, missing or padded provider turn ids, missing or padded prompt/layout lineage, missing or mismatched stored binary/provenance artifact paths, and non-PNG-signature image payloads; `src/lib/production-image-generation-gate.ts` keeps `mock` development-only and revalidates persisted Codex turn/thread ids plus exact persisted binary/provenance paths before production use, so padded persisted artifact paths are blocked rather than trimmed into readiness, and `src/components/deck/GenerateStage.tsx` disables production image generation until a locked project image path decision exists; `src/lib/live-image-provider-adapter.ts` links successful provider output to versioned artifact storage, avoids artifact writes on classified provider failures or storage validation failures, and rejects provider/artifact/storage-contract mismatches with non-retryable `provider_contract` before storage writes; `src/lib/image-artifact-store.ts` stores versioned PNG bytes plus metadata/provenance only after PNG signature validation, rejects mock image artifacts before live storage writes, requires project ids to be safe storage segments before writes, rejects padded prompt/layout lineage, turn/request ids, models, size values, or quality values before sidecar writes, and records a real SHA-256 digest, `src/lib/live-background-batch.ts` rejects non-live, slide-id matched missing-stored-artifact, fake-binary, missing/blank/padded request id or model metadata, stored request provenance id/model/runtime mismatches, stored provider provenance sidecars whose artifact id, production execution mode, provider/auth mode, prompt version, input artifact ids, or duration drift from the stored binary and live artifact, stored binary or sidecar paths that point outside the same versioned slide artifact, duplicate live request/storage identities, structured text overlay strategy drift, or inconsistent five-background batches, `src/lib/slide-generation-queue-executor.ts` records transient retry, cancellation, and partial resume provenance while preventing backoff-time cancellation from starting another attempt and discarding in-flight output after cancellation, `src/lib/slide-generation-queue.ts` regenerates stale prompt-lineage, stale layout-reference, or unfinished resumed slides instead of treating them as completed and records requested/effective/observed concurrency evidence, and `src/lib/live-image-queue-evidence.ts` rejects missing or over-limit concurrency proof plus retry/cancellation evidence that is not anchored to recorded provider jobs, slide-generation prompt usage, matching bundle ids and slide numbers, negative/fractional/non-finite retry delay values, retry delay histories that differ from failed slide evidence, transient failure kinds, final attempt counts/sequences, cancelled jobs without matching slide failures, cancellation attempt counts, and preserved cancel signals, `src/lib/final-slide-compositor.ts` rejects stored background artifact refs that target a different slide than the compositor background, live outside versioned project image storage, or drift between artifact id/path versions, `src/components/deck/review-gallery-model.ts` blocks mock-background, non-image live provider backgrounds, missing-stored-background, malformed or cross-slide background artifacts, compositor SVG/background artifact mismatches, reused stored background artifact identities, missing or invalid editable overlay bounds, fake or reused compositor preview PNGs, or generated-text-collision compositor review evidence, `src/components/deck/ReviewGalleryPanel.tsx` renders compositor thumbnails and the selected presentation preview from stored background artifact references, `src/lib/live-slide-regeneration.ts` requires approved selected originals and preserves approved slide context, `src/lib/live-slide-regeneration-request-validation.ts` blocks blank edit instructions, empty, blank, duplicate, or overlapping keep/change targets and missing or padded approved-original background evidence, `src/lib/live-slide-regeneration-slide-spec.ts` requires regenerated candidates to keep the approved request slide spec hash, `src/lib/live-slide-regeneration-candidate.ts` requires a new background artifact version whose stored version, binary path, metadata path, provenance sidecar path, provenance artifact id, and input lineage to the approved original background artifact match the candidate slide version, includes regenerated provider turn evidence that is consistent across metadata/provenance and distinct from the original background turn id, requires production Codex OAuth non-fixture provenance, and rejects invalid full-slide regeneration candidates, `src/lib/live-slide-regeneration-approval.ts` preserves the approved original unless matching before/after comparison evidence binds the current approved slide, regenerated ready candidate, slide versions, descriptors, new background artifact id, requested changes, preserved targets, and kept preservation checks, `src/lib/live-slide-regeneration-review-evidence.ts` rejects approved review evidence whose embedded comparison drifts from the ready candidate, rejects generic, developer-local, template/sample/example/placeholder, or boundary-whitespace-padded review evidence paths before they can persist into project state, and now records `preserved_after_approval_blocked` evidence when approval is blocked by comparison mismatch, `src/lib/live-generation-report-lineage.ts` blocks missing per-slide lineage, duplicate slide rows, missing, blank, or duplicate source traces, missing, reused, or provider-mismatched text turns, missing text prompt versions, missing or reused image turns, unauthenticated provider provenance, malformed or cross-slide image artifact mismatches, secret-like sidecar lineage fields, mock/fixture markers in exported project content, export compositor mismatches, and secret leakage, and `src/lib/final-export-gate.ts` blocks production export when slide-level live report lineage is missing, incomplete, short of the project slide count, absent from provider provenance, unauthenticated, mismatched against text prompt, turn/request metadata, or when generation report markdown leaks raw secrets; development export contamination now surfaces warnings and a `MOCK MODE` watermark. Lane B recorded real Codex image thread/turn evidence, stored PNG binaries, SHA-256 hashes, metadata sidecars, and provenance sidecars under `projects/df230_live_codex_image`, `projects/df232_live_codex_batch`, and `projects/df235_live_regeneration`; packaged compositor/export/manual QA evidence remains missing.
- `src/lib/image-artifact-store-request-metadata.ts` now rejects empty provider `usage` objects before image bytes, metadata, or provenance sidecars are written, so a supplied usage object must carry at least one usage or cost field before it can count as provider response metadata.
- `src/lib/live-background-batch-storage.ts` now applies the same non-empty provider usage metadata rule to five-background batch stored sidecars, so `{ usage: {} }` cannot satisfy stored request metadata for DF-232 background evidence.
- `src/lib/live-image-queue-concurrency.ts` now rejects zero requested/effective concurrency limits before DF-233 queue evidence can count as ready; `observedMaxRunning` may still be zero for a legitimate no-new-work resume, but runnable throttle limits must be positive.
- `src/lib/live-image-queue-evidence.ts` now rejects blank or boundary-whitespace-padded queue context, job, prompt bundle, retry, and failure identities with `noncanonical_queue_evidence_identity`, so DF-233 evidence cannot pass by copying the same non-canonical job or bundle id through every matching field.
- `src/lib/live-image-queue-retry-delay.ts` now rejects negative, fractional, or non-finite retry delays with `retry_delay_invalid`, so impossible backoff values cannot satisfy DF-233 retry evidence even when job, prompt bundle, slide, and attempt metadata match.
- `src/lib/live-image-queue-evidence-export.ts` now rejects stored image artifact paths that contain non-production markers such as `tmp`, `temp`, `sample`, `example`, `placeholder`, `generic`, or `observer`, so a scratch project PNG path cannot satisfy DF-233 completed-slide storage evidence.
- `src/lib/live-slide-regeneration-request-validation.ts` now rejects boundary-whitespace-padded `mustKeep`/`mustChange` targets with `revision_target_not_canonical`, so DF-235 before/after comparison evidence cannot pass only because target lists are trimmed before matching.
- `src/lib/live-slide-regeneration-approval.ts` now compares before/after approval `requestedChanges` and `preservedTargets` exactly instead of trimming comparison evidence into canonical strings, so a padded target list blocks with `regeneration_comparison_mismatch` and preserves the approved original slide.
- `src/lib/live-slide-regeneration-review-evidence.ts` now rejects scratch or observer-marked review evidence paths such as `projects/tmp/...`, `df235-slide-regeneration-review-generic.json`, or `df235-slide-regeneration-review-observer.json` before they can persist into project state.
- `src/lib/live-generation-report-lineage.ts` now also rejects reused text turn ids, reused text artifact ids, reused image artifact ids, and reused exported PNG hashes across slide lineage rows. The contamination checks live in `src/lib/live-generation-report-contamination.ts`, keeping the main lineage validator below the 250 pure-LOC ceiling while the artifact-identity regressions block a report/export bundle from reusing one Codex text turn, stored text artifact, image artifact, or exported PNG as evidence for multiple slides.
- `src/lib/live-generation-report-lineage.ts` now also rejects slide lineage fields that only become valid after trimming whitespace, including source ids, text artifact ids, text thread/turn ids, text prompt versions, image artifact ids, image provider turn/request ids, and slide prompt versions. This prevents a final report/export lineage bundle from normalizing padded durable ids into production readiness.
- `src/lib/final-export-gate.ts` now also rejects production export summaries whose `artifactHash` is not a full SHA-256 digest and whose export package/project file paths are synthetic, developer-local, or marked as template/sample/example/placeholder evidence, so report/export provenance cannot pass with placeholder hashes such as `sha256:export` or observer-template export paths.
- `src/components/deck/review-gallery-title-edit-export-validation.ts` now blocks otherwise valid Live review gallery evidence unless title edit re-export evidence exists for the reviewed slide, points at a canonical non-synthetic project export SVG rather than boundary-whitespace-padded or template/sample/example/placeholder evidence, contains the edited title instead of a stale title node, and preserves the reviewed compositor export basis plus stored background artifact id/path/hash. The issue remains open because real packaged title edit and re-export QA has not run.
- Research source policy and fetcher tests now preserve `original_url`, `final_url`, `mime_type`, `status_code`, and `content_hash`; `validateResearchSourceCaptureBundle` blocks missing 2 HTML + 1 PDF evidence and missing URL/fetch/MIME/status/archive/text metadata; `ResearchPack.sources[].capture` preserves current capture metadata and `ResearchPack.sources[].captureHistory` preserves recapture history through schema parsing and approved Research Pack artifacts; `SourceReviewList` displays current capture metadata during review; `docs/live-source-capture-bundle.md` records a local 2 HTML + 1 PDF source capture bundle, but the production research surface has not yet produced that bundle or recapture history.
- `src/lib/live-research-evidence.ts` now blocks source-summary-only research claims unless they have a captured source artifact quote span or table reference, blocks Research Packs that have no dataset and no numeric evidence at all, blocks dataset-backed major numbers with missing unit/period/geography/definition metadata or datasets not sourced by the claim's source artifact ids, rejects persisted evidence refs whose claim no longer exists, whose id is duplicated or non-canonical, whose claim/source/dataset ids only become usable after trimming whitespace, whose linked source lacks persisted `capture.rawArchivePath`, whose source artifact path differs from that captured path or only matches after trimming whitespace, or whose dataset id is outside the claim's dataset lineage, and preserves `ResearchPack.liveEvidenceRefs` through schema parsing and approved Research Pack artifacts. `buildDeckPlanPrompt` also excludes live claims with fatal evidence issues when `ResearchPack.liveEvidenceRefs` is present, so direct Deck Plan prompt construction cannot admit source-summary/no-original claims into Usable Research Claims. `src/lib/live-research-evidence-builder.ts` and `src/lib/live-research-pack-builder.ts` can generate those refs from captured source artifact metadata plus evidence extraction results, but no authenticated packaged-app live Research Pack has passed that gate yet.
- `src/lib/live-research-approval-gate.ts` blocks Research Pack approval when live provider provenance is absent, provider provenance does not name the exact canonical current Research Pack artifact id, provider provenance uses a non-`live_research_pack@...` prompt version, any reviewed source lacks complete canonical persisted live capture metadata, any source capture URL/MIME/archive path/hash only becomes valid after trimming whitespace, or a source-strengthening request is still pending, and forwards `approvedResearchPackHash` for deck planning only after approval. The deck-plan handoff helper now returns no handoff for stale approved hashes or approved hashes whose persisted evidence/provenance fail the live gate. `src/lib/live-research-approval-action.ts` now creates the production approval patch and approved research artifact record only after that gate is ready, `ResearchPack.provenanceLineage` preserves provider lineage, and `src/lib/research-review-actions.ts` preserves source exclusions, removes stale persisted live evidence refs for excluded sources, removes numeric evidence that only survives through datasets deleted by source exclusion, clears stale provider provenance after source-exclusion or reinforcement review mutations, and tracks reinforcement requests in `ResearchReviewState`; `ProductionResearchReview` consumes persisted evidence/provenance inputs and can display a persisted Research Pack with source capture metadata, source exclusion/reinforcement controls, `missing_provenance`/`research_pack_provenance_mismatch`/`source_missing_live_capture`/`source_capture_incomplete`/evidence/`pending_reinforcement_request` blockers, and a ready-state approval action that writes the approval log entry with the actual research artifact id/version/path, but the review stage is not yet populated by a live Research Pack QA run.
- `src/lib/project-list-codec.ts` now migrates restart-loaded localStorage projects by parsing stored Research Packs through the current schema and filling missing `ResearchPack.liveEvidenceRefs` / `ResearchPack.provenanceLineage` arrays. Historical projects containing real production provider artifacts still require clean migration QA.
- Structured Codex output is locally gated against partial/schema-invalid output, App Server progress/approval/partial/completion/failure/cancellation events now map into Job Manager state, observed JSON-RPC `turn/started` / `item/agentMessage/delta` / `item/completed` / `turn/completed` notifications can now be converted into internal task events, current nested App Server error notifications map into provider failure events, async production notification streams now flow through `runProductionCodexAppServerJob` into Job Manager accepted-artifact provenance, authenticated App Server health/resume turns now exist, and real protocol-level App Server structured turns have completed with schema-constrained JSON, streamed deltas, token usage, and durable thread/turn ids. This satisfies DF-211 common runner acceptance; packaged production text execution remains tracked by DF-213, DF-214, and DF-215. `src-tauri/src/codex_app_server_smoke.rs` now registers a desktop `deckforge_codex_app_server_smoke` command, `src-tauri/src/codex_app_server_structured_turn.rs` registers `deckforge_codex_app_server_structured_turn`, `src/lib/desktop-app-server-bridge.ts` parses both evidence shapes through Zod, `src/lib/desktop-codex-app-server-production-job.ts` feeds desktop structured-turn notifications into the production Job Manager path, `src/lib/desktop-live-interview-workflow.ts` wires the interview `questions` and `brief` desktop turns into artifact persistence, `src/lib/desktop-live-text-pipeline-workflow.ts` wires Plan/Design/Layout desktop structured turns into persistence, `src/components/deck/ProductionTextWorkflowLauncher.tsx` connects the ready interview and production text-pipeline buttons to those launchers while preserving ready artifact records in the project patch, and `src/lib/live-text-production-workflow.ts` runs production App Server jobs before converting accepted outputs into DeckForge live text artifact records and project patches. Library-level live interview workflow runs produced `questions` artifact `p_goal_live_interview_20260619_fixed_2_questions_live` on thread `019edbeb-0963-7de1-a9e6-654f708a5637`, turn `019edbeb-0baf-71e3-85be-a4c331202d4b`, and a ready `questions`/`brief` bundle with turns `019edc17-b011-74d2-ae54-49842b7abd9d` and `019edc19-d06e-7793-9fbc-80ec053bb9fa`; a library-level live text pipeline run produced `deck_plan`, `design_system`, and `layout_ir` artifacts on turns `019edbf8-dce2-7a51-90ff-6fdb46137aaa`, `019edbfa-171c-7983-b2b8-33de3ead05f3`, and `019edbfb-55b5-7973-b2e2-a9825d7aa9d4`; the app still lacks a recorded packaged run against authenticated live turns.
- `src/lib/live-interview-cutover.ts` now blocks interview question and Brief approval unless artifacts have production Codex provenance, normalized-distinct question/Brief turn ids, normalized-distinct question/Brief artifact ids, a canonical distinct user-answer bundle input, interview-specific prompt versions, question artifact project/initial-prompt input lineage (`question_missing_project_input`), actionable nonblank follow-up question text (`invalid_follow_up_question`), and no mock/fixture contamination. Missing required answers produce an `interview_follow_up@v1` live turn requirement, provider failure recovery offers retry/manual input without fixture fallback, and the production text workflow converts accepted question/Brief jobs into persisted live artifact records plus an interview project patch. The desktop `questions` and `brief` jobs now send App Server strict response schemas, `src/lib/live-interview-answer-map.ts` hands draft-Brief answers to the desktop launcher, a library-level live run persisted one question artifact and eight follow-up questions, a later library-level live run persisted a ready question/Brief bundle with Brief input lineage, and the production interview UI shows the required live `questions`/`brief` turns plus App Server bridge blocker while preserving ready question/Brief artifact records with the Brief patch; it still has not recorded authenticated packaged-app follow-up or Brief artifact bundles.
- The DF-213 interview provenance gate now also rejects non-canonical question/Brief artifact identities with `noncanonical_interview_identity`, so padded artifact ids, turn ids, or thread ids cannot satisfy durable live interview evidence.
- The DF-213 interview provenance gate now also rejects non-canonical question and answer input artifact ids with `noncanonical_interview_input_identity`, so padded answer-bundle ids cannot be normalized into valid follow-up or Brief lineage.
- `src/lib/live-text-pipeline-cutover.ts` now blocks Deck Plan, Design System, and Layout IR approval unless each artifact has normalized-distinct production Codex turn provenance, normalized-distinct persisted artifact ids, required input lineage, schema-valid markdown/JSON output, component-catalog Layout IR, canonical shared `deckContextId`, canonical shared `designSystemId`, and no fixture fallback. Schema failures can request at most two live repair turns before `schema_repair_exhausted` blocks approval, and the production text workflow persists accepted Plan/Design/Layout jobs only after the cutover gate is ready. The production Plan/Design/Layout UI now shows the required `deck_plan`/`design_system`/`layout_ir` turns plus upstream and bridge blockers, the ready button is wired to a desktop structured-turn launcher that can persist accepted Plan/Design/Layout outputs and their live artifact records, the Design System/Layout IR jobs now send App Server strict response schemas, and a library-level live run persisted Plan/Design/Layout artifacts from real App Server turns, but the packaged app surface still has not recorded authenticated live planning, schema repair, design, or Layout IR artifact bundles.
- The DF-214 text-pipeline gate also requires stage-specific prompt versions for Deck Plan, Design System, and Layout IR artifacts, including the desktop prompt variants, and now requires canonical Deck Plan/Design/Layout handoff input ids plus distinct approved Brief and approved Live Research Pack artifact ids, so a structurally valid Codex artifact from another text stage, a missing, reused, or padded Research handoff, or whitespace-padded reused evidence cannot satisfy the cutover.
- The DF-214 text-pipeline provenance gate now also rejects non-canonical Plan/Design/Layout artifact identities with `noncanonical_text_pipeline_identity`, so padded artifact ids, turn ids, or thread ids cannot satisfy durable planning bundle evidence.
- The DF-214 text-pipeline provenance gate now also rejects non-canonical handoff input artifact ids with `noncanonical_text_pipeline_input_identity`, so padded Plan/Design/Layout input lineage cannot be normalized into valid stage handoffs.
- The DF-215 smoke gate now requires stage-specific prompt versions across `questions`, `brief`, Deck Plan, Design System, and Layout IR, requires question initial-prompt input lineage, Brief answer-bundle input lineage, Deck Plan Research Pack input lineage distinct from Brief, rejects reused text artifact ids or turn ids across smoke stages, requires post-resume evidence to run on the same thread as the previous text artifact turn, and normalizes resume turn ids before checking for reused text artifact turns, so a complete live Codex bundle cannot pass if one artifact was generated with another text stage's prompt, skipped the project/answer/research inputs, reused one artifact or turn as multiple stages, used a mismatched resume thread, or reused a whitespace-padded resume `nextTurnId`.
- The DF-215 smoke gate now also rejects non-canonical text artifact identities with `text_artifact_noncanonical_identity`, so padded artifact ids, turn ids, or thread ids cannot satisfy durable packaged smoke evidence.
- The DF-215 smoke gate now also rejects non-canonical text artifact input identities with `text_smoke_noncanonical_input_identity`, so padded project, answer-bundle, Research Pack, or stage handoff input ids cannot satisfy packaged smoke lineage.
- Project thread recovery is locally gated by one canonical coordinator thread, duplicate worker stage rejection, nonblank unique canonical worker thread ids distinct from the coordinator thread, shared worker context hash, canonical approved artifact bundle equality, canonical recovered worker `lastCompletedTurnId`, raw conversation source-of-truth rejection including nested worker resume/source metadata, same-context-id context-hash drift or missing-job-hash stale-job detection when the current context hash is known, and `evaluateProjectThreadResumeEvidence` blocking `resume_previous_turn_not_recovered`, non-canonical resumed turn ids as `resume_next_turn_not_canonical`, plus whitespace-padded recovered-turn reuse as `resume_reused_existing_turn`. A library-level live App Server recheck resumed recovered worker thread `019edc28-bf27-7380-b7d2-65405e6c6758` from turn `019edc28-c179-7453-a5a5-c87e29096422` to turn `019edc28-f9ec-72e1-9695-1a9a2c2ca61d` after recreating the App Server process, but no packaged desktop restart/reopen run has resumed a production UI worker thread yet.
- `src/lib/live-golden-path-e2e.ts`, `src/lib/live-golden-path-evidence-path.ts`, and shared `src/lib/live-evidence-path.ts` block unsigned, timestamp-less, or digest-mismatched reports, missing or out-of-order Golden Path steps, missing production Codex text-stage lineage, generic `plan`/`design`/`layout` labels used as Deck Plan, Design System, or Layout IR text-stage proof, missing per-step screenshots, synthetic, template/sample/example/placeholder, developer-local, or remote URL-only report/screenshot/recording/final validation bundle paths, incomplete, duplicate-referenced, or unexpected-referenced final validation bundle manifests, insufficient, duplicate, placeholder, reserved documentation, local, private-network, or normalized-duplicate source URLs/artifact ids, unauthenticated images, image artifacts missing model/runtime or prompt version evidence, duplicate live image artifact/provider turn ids, fewer than five distinct initial live image artifacts, missing approved full-slide regeneration image evidence, regenerated artifacts whose input lineage only matches an initial image after trimming, regenerated artifacts counted toward the initial five-image floor, missing, timestamp-less, or pre-signature restart/reopen evidence, mock/fixture contamination, and secret-like report content, but no real Golden Path run has produced the required bundle.
- A fresh DF-235 live regeneration rerun produced `projects/df235_live_regeneration_lineage_20260622/slides/images/slide_003.v2.png` and a provenance sidecar whose `inputArtifactIds` include the approved original `df232_live_codex_batch_image_slide_003_v1`. This gives the image lane a current lineage-valid regenerated candidate, but it is still not a packaged Review-stage approval or failed-regeneration preservation run.
- A DF-235 product writer smoke replayed that lineage-valid regenerated candidate through `approveReviewStageRevisionWithEvidence` and produced approved review evidence at `projects/df235_live_regeneration_lineage_20260622/live-evidence/df235-slide-regeneration-review-rev_df235_lineage_20260622.json`, but it is still not a packaged Review-stage UI run and does not prove failed-regeneration preservation.
- `scripts/produce-df235-packaged-review-evidence.ts` now provides the handoff
  surface for a real packaged Review-stage bundle. It parses copied
  `docs/live-evidence/...` approval and failed-regeneration preservation JSON,
  requires the same packaged review session and package hash, and checks visible
  before/after plus preservation display artifacts before returning ready.
  Missing approval proof, missing failure-preservation proof, reused paths,
  wrong-session artifacts, or display-only claims remain blocked.
- `src/lib/live-benchmark-evidence.ts`, `src/lib/live-benchmark-package-hash.ts`, and `src/lib/live-benchmark-evidence-path.ts` block missing, duplicate, or unknown benchmark scenarios, missing, malformed, or non-canonical uppercase package archive SHA evidence, top-level benchmark report paths outside exact `docs/live-benchmark-report.md`, synthetic, template/sample/example/placeholder, or developer-local output bundle/report/screenshot/Golden Path paths, synthetic or observer-template source/image/request/regeneration/export artifact ids, reused scenario report paths, output bundles from a different package candidate, output bundle/report/Golden Path/screenshot paths that borrow another DF-242 scenario id, missing/duplicate/incomplete output bundle manifests, mismatched output bundle evidence counts, duplicated Golden Path report/screenshot/source/image artifact references within or across passed benchmark runs, screenshot evidence that does not name every Golden Path step, missing separate regenerated image evidence, regenerated images counted toward the five initial image floor, duplicate or missing live image provider turn/request evidence within or across passed benchmark runs including mixed turn/request bundles, reused final export artifact ids, mock score contamination, unsupported or unclassified failures, passed runs that retain failure domains, incomplete Golden Path runs, and fewer than four passing named Live benchmarks, but no real benchmark output bundle set has been produced.
- Golden Path and benchmark evidence paths are now canonical before they can count: boundary-whitespace-padded report, screenshot, recording, validation bundle, output bundle, scenario report, and Golden Path report paths are rejected instead of being trimmed into readiness. Benchmark final export artifact ids are also canonical nonblank values, so a padded export id cannot satisfy DF-242 output bundle readiness.
- `src/lib/live-interruption-matrix.ts`, `src/lib/live-interruption-image-resume.ts`, `src/lib/live-interruption-evidence-path.ts`, `src/lib/live-interruption-scenario-evidence-path.ts`, and `src/lib/live-interruption-report-path.ts` block missing, duplicated, or unknown scenarios, synthetic, template/sample/example/placeholder, developer-local, reused live job, reused recovery snapshot evidence, recovery snapshot paths that identify a different scenario, or non-canonical live job/cancel/gate evidence identities including dot-segment path aliases, missing snapshot/cancel signal evidence, top-level matrix report paths outside exact `docs/live-interruption-matrix.md`, synthetic or developer-local matrix report paths, recovered job states outside the DF-243 taxonomy, boolean-only cancel signal claims without a persisted JSON path, reused app cancel snapshot/cancel signal evidence paths, boolean-only interrupted approval/export checks without persisted gate JSON paths, reused approval/export gate evidence paths, cancel signals that target a different live job id, missing app-storage cancel snapshots, missing interrupted approval/export gate checks, unsafe text/fetch recovery, completed artifact loss, unsafe partial image resume including blank, duplicated, completed, missing-pending, or outside-pending resumed image ids, cancelled background jobs, cancelled jobs that still complete artifacts, and interrupted artifact approval/export. `src/lib/live-image-queue-restart-resume-evidence.ts` also rejects duplicate artifact ids inside restart-resume completed-before, completed-after, pending, or resumed artifact arrays plus resumed artifacts that already appeared in `completedArtifactIdsBefore`, before DF-233 queue resume proof can count. `src/lib/live-interruption-closure-evidence.ts` and `src/lib/live-interruption-closure-payload.ts` also block closure manifests that name another issue or ticket, whose matrix payload is missing, malformed, or borrowed from another matrix evidence path, or whose required artifacts remain generic `recovery/*.json` paths instead of committed `docs/live-evidence/...` bundle entries. A direct authenticated App Server probe started text turn `019edc5a-0cc0-7031-915a-5fc6d65c6d86` on thread `019edc5a-0a48-7ec2-aa86-c539ed9546b1`, sent `turn/interrupt`, and observed `interrupted` in `turn/completed`, `thread/read`, and `thread/turns/list` with evidence digest `27855e9afff031bc49c87bb08bb46ea6ac9a5436e4a2eef9ecb74382e62809b6`. A live `fetchResearchSource` GET to `https://httpbin.org/delay/10` was aborted after 754 ms and returned retryable `failed` without raw content or content hash, digest `a472a031283e5a2ce537801d43a15b2d121241d823397868b81437c50e78bc3d`. A second App Server interrupt probe observed turn `019edc60-6250-7ba1-bdc4-86c28083c19d` for 3,009 ms after completion and saw 0 late `item/completed` notifications, digest `f35c082c75b37ccbe7e8e5eddf1907e61e66171e13d94dd2c4df50fe3060b62f`; the image partial-resume, app-level persisted cancel snapshot, and interrupted approval/export scenarios still have not run.
- `src/lib/live-usage-summary.ts` blocks missing, blank, or duplicated usage stage ids, unsupported runtime provider kinds, missing provider usage summaries, empty provider-supplied usage objects, incomplete Codex token pairs, image usage evidence without image counts, invalid usage/cost amounts, zero-duration or otherwise invalid duration/retry data, unsupported runtime cost labels, unlabelled estimated costs, estimated costs labelled as exact charges, missing image Codex usage confirmation, API-key-required image disclosure, and Codex image usage confirmation without persisted non-synthetic, non-local `confirmationEvidencePath` JSON evidence tied to the same provider job id, while avoiding false image-billing blockers for complete Codex text usage that happens to use `stageId: "generate"`; `src/lib/live-usage-billing-evidence.ts` also requires `apiKeyRequired: false` and rejects boundary-whitespace-padded, fallback `unknown` project/job, template/sample/example/placeholder, generic usage confirmation JSON paths that do not end in `image-billing-confirmation.json`, or confirmation paths copied from a different job before summary approval, formatted summaries, audit report lines, or progress display. `src/lib/live-usage-billing-same-job.test.ts` locks that same-job regression across summary, app progress, and audit report surfaces. `src/lib/live-app-server-usage-summary.ts` maps App Server `thread/tokenUsage/updated` notifications into text usage summaries and preserves malformed supplied numeric usage/cost fields as invalid evidence instead of silently dropping them; `formatLiveUsageSummary` and `src/lib/audit-log.ts` render `estimatedCostUsd` as `cost estimate`, redact image usage disclosure labels, and render evidence-less or API-key-required confirmed-looking image usage as not confirmed; and `src/components/deck/ProviderJobProgressPanel.tsx` now renders provider id, measured duration, retry count, valid usage items, estimated provider cost labels, confirmed Codex image usage disclosure labels only with `apiKeyRequired: false`, persisted non-local evidence, matching provider job id, and user confirmation, unconfirmed/evidence-less/API-key-required/developer-local/template/generic/non-canonical Codex image usage status, wrong-job confirmation status, and redacted secret-like usage label text in the app progress surface while omitting invalid provider payload amounts from visible usage rows. Live Codex usage probe turn `019edc53-3bfe-76d3-912d-31769ee3fd3f` produced 25,006 input tokens and 141 output tokens, but packaged app-surface usage summary manual QA with real provider image Codex usage payloads is still missing.
- `src/lib/live-usage-stage-identity.ts` now rejects missing, blank, non-canonical, or duplicated usage stage ids, so a whitespace-padded `generate` stage cannot bypass exact generate-stage image confirmation checks while still rendering as displayable after trimming.
- `src/lib/live-image-billing-confirmation.ts`, `src/lib/live-image-billing-job.ts`, and `src/components/deck/GenerateStage.tsx` now give production `codex` image-generation jobs a product path to persist the pre-generation Codex OAuth image usage confirmation JSON, mirror it into project artifact storage at `projects/<project>/usage/<project>/<job>/image-billing-confirmation.json`, attach it to the job usage summary before generation starts, and cancel the queued image job if the evidence write fails. `src/lib/desktop-codex-image-generation.ts` now also sends the App Server image response schema as a strict object with `additionalProperties: false`; without that field, the live image turn failed before generation with `invalid_json_schema`. Long-running image turns now request a 600000ms structured-turn timeout through `src/lib/desktop-app-server-bridge.ts` and `src-tauri/src/codex_app_server_structured_turn.rs`, because real image runs have exceeded the previous 180s Tauri bridge limit and the latest product-run rerun took 185181ms. The product-run smoke summary at `docs/live-evidence/codex-image/df244-generate-export-smoke-20260622/summary.json` completed one real Codex image through the Generate runner and exported same-job image, billing, and queue artifacts. DF-244 still needs a packaged-app run to capture that actual persisted confirmation record alongside real image usage payloads and manual display evidence.
- The DF-244 product-run usage summary at `docs/live-evidence/codex-image/df244-generate-export-usage-summary-20260622.json` proves the Lane D confirmation resolver can consume that persisted product confirmation and produce `confirmed_app_surface_pre_generation_codex_oauth`, but it is still not packaged UI display evidence.
- `scripts/produce-df244-packaged-usage-evidence.ts` now provides the handoff
  surface for a real packaged usage bundle. It parses the product run summary,
  usage summary, persisted Codex OAuth billing confirmation record, and
  screenshot/recording display proof, then runs `evaluateLiveUsageSummary`
  against the derived same-job image stage. Missing display proof, wrong-job
  confirmation records, API-key-required disclosures, or malformed input remain
  blocked before they can be recorded as DF-244 release evidence.
- `scripts/lane-d-live-usage-confirmation.mjs` now keeps the Lane D packaged usage bundle evidence-driven: it changes `df244-usage-summary.json` to `confirmed_app_surface_pre_generation_codex_oauth` only after it finds a canonical persisted `usage/<project>/<job>/image-billing-confirmation.json` record whose internal `evidencePath` matches the product storage path. The current committed Lane D bundle remains blocked because that packaged-app confirmation JSON is not present yet.
- `src/lib/production-packaging-evidence.ts` and `src/lib/production-packaging-evidence-payload.ts` block missing, synthetic, dry-run, transient `tmp`/`temp`/observer, or developer-local package archive, native bundle, runbook, or persisted `releaseTrustEvidencePath` evidence, including non-canonical clean-machine runbook paths, developer-local persisted `releaseTrustEvidencePath` JSON bundles, release-trust evidence paths that do not identify a release-trust bundle, non-canonical release-trust path strings with boundary whitespace, generic release-trust paths without codesign/notarytool/stapler/`spctl` assessment markers, release-trust payloads that omit matching package/native hashes or passed zero-exit codesign/notarytool/stapler/spctl assessment records, missing package/native macOS bundle hashes, missing, placeholder, malformed, non-canonical, or boundary-whitespace-padded Developer ID TeamIdentifier evidence, missing notarization/stapling, Gatekeeper rejection, non-production package evidence, package content contamination, invalid, missing, duplicated, evidence-pathless, cross-step, reused-path, boundary-whitespace-padded, or payload-drifted clean-machine step evidence, missing/generic/non-canonical/developer-local/boundary-whitespace-padded clean macOS account evidence, clean-machine account payloads that still identify the developer account, and missing runtime remediation. Clean-machine summaries count valid distinct checklist steps only, so repeated, cross-step, reused-path, padded, unsupported, or payload-drifted step labels cannot inflate coverage. The native DMG has been regenerated and scanned clean, but fresh macOS account validation, Developer ID signing, notarization, persisted release-trust assessment bundle capture, and Gatekeeper acceptance have not been recorded.
- `src/lib/production-packaging-evidence-payload.ts` now also requires each clean-machine step payload to carry the same `macosUsername` and `homeDirectory` as the clean macOS account payload, so a step captured from the developer account or a different local account cannot satisfy DF-245 just because it references the canonical `cleanMachineAccountEvidencePath`.
- `src/lib/live-manual-qa-evidence.ts`, `src/lib/live-manual-qa-slide-actions.ts`, `src/lib/live-manual-qa-approval-targets.ts`, `src/lib/live-manual-qa-issue-log.ts`, and `src/lib/live-manual-qa-session-evidence.ts` block developer self-test evidence, missing, synthetic, boundary-whitespace-padded, outside-`docs/live-evidence`, temporary/observer session, generic/non-session, generic session bundles, generic session-notes, template/sample/example/placeholder, developer-local absolute, or `file://` persisted `sessionEvidencePath` bundles, session JSON payloads that are absent, malformed, or borrowed from another `sessionEvidencePath`, zero-duration or over-10-minute first-use setup, missing, misunderstood, or duplicated approval targets, missing, invalid, placeholder, reserved-documentation, private-network, local, boundary-whitespace-padded, or final-report-unlinked real-source opens, padded final-report source URLs that only match after trimming, missing regeneration/title-edit/export actions, boundary-whitespace-padded, arbitrary non-`slide-<number>`, or placeholder/template/mock/fixture slide ids used as regeneration or title-edit evidence, invalid slide action ids mixed into otherwise valid regeneration/title-edit lists, invalid manual QA counters, P0 issues, mock indicators, placeholder outputs, empty severity issue-log entries, and missing severity issue lists. Manual QA summaries count distinct approval targets and canonical non-placeholder slide ids only, so repeated checks, padded slide ids, placeholder slide ids, arbitrary tokens, or mixed contaminated slide action lists cannot inflate coverage, and a copied session path cannot borrow another session's payload; no non-developer manual QA session evidence bundle has been recorded.
- `scripts/produce-df246-packaged-manual-qa-evidence.ts` now provides the
  handoff surface for that real non-developer session bundle. It parses the
  captured session payload, verifies it against the current package hash, runs
  `evaluateLiveManualQaEvidence`, and keeps developer self-tests, package-hash
  drift, missing session bundles, or incomplete QA actions blocked before they
  can feed the packaged evidence index.
- `src/lib/packaged-live-evidence-index.ts` blocks missing, duplicated, wrong-issue, non-canonical, wrong-ticket, ticket-id substring-only paths, duplicate-path, package-hash drift, malformed-hash, or validation-blocked shared Packaged Live evidence entries for DF-205, DF-233, DF-235, DF-241, DF-242, DF-243, DF-244, DF-245, DF-246, and DF-247. `docs/live-evidence/release/packaged-live-evidence-index.json` now materializes the shared index with blocked entries, per-entry package SHA-256 evidence, and verified entry SHA-256 digests, while `src/lib/packaged-live-evidence-index-artifact.test.ts` checks the committed index, evidence file hashes, and child evidence package hashes. `bun run evidence:packaged-index:produce -- docs/live-evidence/release/packaged-live-evidence-index.json [generatedAt]` regenerates that index from the ten child evidence JSON artifacts instead of requiring manual hash edits. It still blocks DF-247 release-index readiness when any upstream packaged evidence entry is blocked, so the final release cannot claim readiness before auth/secret lifecycle, queue, regeneration, usage, Golden Path, benchmark, interruption, packaging, and manual QA artifacts are actually ready.
- `src/lib/live-release-gate.ts`, `src/lib/live-release-lineage-gate.ts`, `src/lib/live-release-final-export-gate.ts`, `src/lib/live-release-p0-gate.ts`, and `src/lib/live-release-decision-gate.ts` block DF-247 release when any P0 ticket is not `Verified Live`, P0 ticket evidence contains contradictory duplicate statuses (`p0_ticket_status_conflict`), production mock execution is enabled, package scans are contaminated, benchmark summary evidence contains unsupported DF-242 failure-domain taxonomy values (`live_benchmark_invalid_failure_domain`) or contradictory duplicate scenario outcomes (`live_benchmark_status_conflict`), the five named DF-242 benchmark scenarios are not all present, fewer than 4 named Live benchmarks pass with `failureDomain: "none"`, Golden Path lineage is missing, does not include a non-synthetic final export artifact (`golden_path_export_missing`), includes a final export artifact that is not from a production Codex session (`golden_path_export_not_live`), has missing final export model/runtime or prompt version evidence, has noncanonical final export turn/thread ids, or has mock/fixture artifacts, the critical defect counter is invalid, critical defects are open, unresolved P1 category or summary evidence involves data loss/security/billing/payment/source/citation errors, the canonical release decision path is not `docs/live-release-decision.md`, the cited `live_release_decision` payload is absent, malformed, or borrowed from another decision state, the decision remains `Blocked` (`release_decision_blocked`), or known limits are not recorded (`missing_known_limits`). `bun run evidence:df247:produce -- <release-gate-input.json> [output.json]` now turns a captured release-gate bundle plus the packaged evidence index into DF-247 evidence while preserving blocker codes and references, and `bun run evidence:df247:current -- docs/live-evidence/release/df247-evidence.json [generatedAt]` regenerates the current blocked release-gate artifact from the committed packaged index without creating a circular index SHA dependency.
- Existing release docs before this file are internal MVP/mock-provider scoped.
- The production `codex` Generate stage now routes through the desktop Codex
  image client, live slide-generation queue, and image artifact storage contract
  rather than the mock slide loop. This improves release readiness, but the
  release decision stays `Blocked` until the same path is proven from the
  packaged app with Golden Path, benchmark, interruption, usage, clean-machine,
  and manual QA evidence.

## Required evidence to unblock

1. Run production-mode provider and provenance tests.
2. Reproduce App Server bootstrap/restart evidence on a clean macOS account and run the DeckForge production text pipeline through authenticated live App Server turns, including interview questions, required follow-ups, Interview Brief, Deck Plan, Design System, Layout IR, and schema repair artifacts.
3. Wire and validate source capture bundle and recapture-history persistence from the production research surface.
4. Produce a live Research Pack review with app-populated `ResearchPack.liveEvidenceRefs`, `ResearchPack.provenanceLineage`, and approval provenance.
5. Produce five initial live image artifacts with shared `deckContextId`, shared `designSystemId`, matching layout screenshot references, turn metadata, live queue retry/cancellation/resume evidence, live compositor screenshots, one approved live full-slide regeneration artifact that is regeneration-marked and input-linked to an initial live image, and a per-slide generation report with text turn, image turn, prompt version, and compositor-matched export hashes.
6. Complete Live Golden Path E2E and save `live_e2e_report.md`.
7. Run the live interruption matrix for text, fetch, image partial resume, cancellation, and interrupted artifact approval/export.
8. Run usage summary manual QA with real provider usage/cost payloads and image usage disclosure.
9. Run clean-machine package validation from a fresh macOS account.
10. Run the Live manual QA checklist.
11. Replace the blocked Packaged Live evidence index entries with ready DF-205, DF-233, DF-235, DF-241, DF-242, DF-243, DF-244, DF-245, DF-246, and DF-247 evidence artifacts and their SHA-256 digests after the upstream artifacts are genuinely ready.
12. Re-run the DF-247 release gate and update this decision plus known limits.

## 2026-06-21 Runtime/Text App-Surface Recheck

The Runtime/Text lane ran the production browser app surface against authenticated `codex app-server --stdio` through a Tauri-compatible bridge helper. The interview route completed live `questions` and `brief` App Server turns and persisted app records for project `p_live_runtime_text_20260621`; the ready records were `interview_questions` hash `sha256:29b8b76d` on thread `019ee652-f5e8-7eb2-b61b-6eadaba4307e`, turn `019ee652-f878-73c3-ae5d-80a212086a04`, and `interview_brief` hash `sha256:bd4566a1` on thread `019ee653-1eea-7e83-8a5c-5a0e786b8b4b`, turn `019ee653-2126-7b51-bc89-f0d16497dc2c`.

The Plan route remained blocked before any Deck Plan turn launched because no real approved live Research Pack exists. A synthetic Research Pack with canonical local hash `sha256:fc483c37` still failed the handoff because it lacked live Research approval provenance, live evidence refs, and live source capture metadata. The production text gate has been fixed to block that state before launch. Release remains `Blocked`; this recheck improves DF-213 evidence but does not unblock DF-214, DF-215, DF-241, DF-243, or DF-247.

## 2026-06-21 Lane I Release Gate Recheck

Lane I ran from `/Users/jake/chatgppt-lane-auth-release-qa/deck-scribe-craft-07`
on branch `jacobex/live-lane-auth-release-qa`. The release decision remains
`Blocked`.

Fresh evidence:

- Non-destructive auth/runtime probe: `codex-cli 0.141.0`,
  `codex login status` returned `Logged in using ChatGPT`, and
  `codex app-server daemon version` returned `status: "running"` with
  `cliVersion: "0.141.0"` and `appServerVersion: "0.141.0"`.
- `bun run package:dry-run` passed after `bun install` restored missing
  dependencies; fresh dry-run archive SHA-256
  `e80f2378b21a79b5e600e49840deb97e6159d249e4d45d50ad9f19699a6a680f`, 288,577
  bytes, 26 archive members, 17 app files.
- Dry-run package scans found 0 mock provider/stage, fixture/test path,
  local-path, `.omx`, `.playwright-mcp`, `CODEX_SESSION=`, long `Bearer`,
  assigned `OPENAI_API_KEY`, OpenAI key-shaped, bundled `auth.json`, or bundled
  `.codex` hits.
- `bun run tauri:build` passed; the fresh DMG was copied to
  `release-artifacts/DeckForge_0.1.0_aarch64.dmg`, and checksum verification
  returned `OK` for SHA-256
  `33cc5cb29e25aba266288546037bd5f5007f7696a6a65bfb4787bd1aa50b2f20`.
- Native app binary SHA-256:
  `ae4f2216b92e03254cf9522406d7fc9b9d66c374a428858c055af7dd2feb6fd7`.
- Native bundle scans found 0 configured mock/fixture/test/local-path/secret
  hits.
- `security find-identity -v -p codesigning` found 0 valid identities.
- `xcrun notarytool history` failed with `Must provide credentials`.
- Native app signing remains ad-hoc with `TeamIdentifier=not set`;
  `codesign --verify --deep --strict` fails with `code has no resources but
signature indicates they must be present`.
- The DMG is unsigned; `spctl` rejects it with `source=no usable signature`.

DF-247 cannot close. P0 prerequisites are still not all Verified Live, live
benchmark count remains below 4 of 5, no real Golden Path final export lineage
bundle exists, DF-245 lacks signed/notarized/Gatekeeper/clean-machine evidence,
DF-246 lacks non-developer manual QA evidence, and the decision must stay
`Blocked`.

## 2026-06-22 DF-246/DF-247 Evidence Handoff

The manual QA release evidence now explicitly ties the current DF-246 checklist
to the active package archive
`e6ed0e25791dd51a1c206247bd0faf5a1010aaee6c7b16e7256dfd25f74f47f6` and the
current package/signing blocker recheck at
`docs/live-evidence/release/df245-package-recheck-20260622.json`. This is a
handoff basis only, not a pass result: no non-developer tester session bundle has
been captured.

The DF-247 release-gate evidence and Packaged Live evidence index were refreshed
again at `2026-06-22T10:21:00.000Z` through the current DF-247 generator and the
packaged-index generator, preserving the release-gate blocker codes/refs instead
of a manual blocker summary. Release remains `Blocked` until the upstream
packaged evidence entries become ready and the canonical release decision is
approved.

## 2026-06-22 DF-241/DF-242 Candidate Evidence

The current evidence collector at `scripts/collect-df241-df242-candidate-evidence.ts`
materializes a blocked candidate bundle at
`docs/live-evidence/release/df241-df242-candidate-20260622.json`. It proves the
existing live text, source, image, regeneration, and export artifacts are
available for review, but also proves they are not a signed packaged Golden Path
or five packaged benchmark runs.

`scripts/produce-df241-df242-packaged-evidence.ts` now provides the handoff
surface for real packaged-run JSON. It parses the DF-241 Golden Path bundle and
DF-242 benchmark bundle at the JSON boundary, evaluates them through the
existing release validators, and emits `df241-df242-packaged-run-evidence`
without synthesizing readiness from partial artifacts. Malformed or incomplete
input fails before it can be recorded as release evidence.

Release remains `Blocked`: DF-241 still lacks packaged login/title-edit,
signed report, screenshots/recording, final validation bundle, and restart
evidence; DF-242 still has 0 of 5 passed live benchmarks and no output bundles.

## 2026-06-22 DF-245 Dry-Run Launch Smoke

The current unsigned dry-run app launcher was executed from a temporary HOME and
served the app root at `http://127.0.0.1:4186/` with HTTP 200, verified app
shell text, and returned HTTP 200 for the packaged JS/CSS asset URLs referenced
by the HTML. The evidence is recorded at
`docs/live-evidence/release/df245-dry-run-launch-smoke-20260622.json`.

Release remains `Blocked`: this is developer-worktree dry-run evidence only, not
Developer ID signed, notarized, stapled, Gatekeeper-accepted, or clean-machine
package evidence.
